{ "params": 
    {"get": [
      { "subparam": "nodes",
        "help": "Get the list of nodes on the docker swarm",
        "action": "docker get nodes"

      },
      { "subparam": "networks",
        "help": "Get the list of networks on the docker swarm",
        "action": "docker get networks"
      }
    ],
    "create": [
      { "subparam": "nodes",
        "help": "Create a new node for the docker swarm",
        "action": "docker get nodes"

      },
      { "subparam": "networks",
        "help": "Create a new network on the docker swarm",
        "action": "docker get networks"
      }
    ]
  }
} 
