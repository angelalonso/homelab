{ "params": 
    {"get":
      { "nodes": {
        "help": "Get the list of nodes on the docker swarm",
        "action": "docker get nodes"
        },
        "pods": {
        "help": "Get the list of pods on the docker swarm",
        "action": "docker get pods"
        }
      },
     "create":
      { "nodes": {
        "help": "Create the list of nodes on the docker swarm",
        "action": "docker get nodes"
        },
        "pods": {
        "help": "Create the list of pods on the docker swarm",
        "action": "docker get pods"
        }
      }
  }
}
