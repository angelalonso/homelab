{% for host, values in secrets.hosts.items() %}
{{ host }} ansible_ssh_host={{ values.ip }} ansible_ssh_user={{ values.ansible_ssh_user }} ansible_ssh_pass={{ values.ansible_ssh_pass }}
{% endfor %}

{% for group in groups %}
[{{ group }}]
  {% for host in groups[group] %}
{{ host }}
  {% endfor %}
{% endfor %}

