{% for host, values in secrets.hosts.items() %}
{{ host }} ansible_ssh_host={{ values.ip }} ansible_ssh_user={{ values.ansible_user.name }} {% if values.ansible_user.ssh_key is defined %}ansible_ssh_private_key_file={{ values.ansible_user.ssh_key }}{% else %}ansible_ssh_pass={{ values.ansible_user.password }}{% endif %}

{% endfor %}

{% for group,values in secrets.groups.items() %}
[{{ group }}]
  {% for host in values.hosts %}
{{ host }}
  {% endfor %}
{% endfor %}

